<Window x:Class="Task16.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task16.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Style="{StaticResource HeaderStyle}">Таблицы</TextBlock>
            <Button
           Grid.Column="1"
           Content="Клиенты"
           Command="{Binding OpenClientCollectionWindow}"/>
            <Button
           Grid.Column="2"
           Content="Заказы"
           Command="{Binding OpenOrderCollectionWindow}"/>
        </Grid>
    </Grid>
</Window>
